# 函数高级
## 函数默认值
`返回值类型 函数名(参数 = 默认值)`


如果函数声明有默认参数，函数实现就不能有默认参数
即声明和实现**只能有一个有**默认参数

函数的占位参数 `返回值类型 函数名（数据类型）{}`

## 函数重载
满足条件：
1. 同一个作用域下
2. 函数名相同
3. 参数不同（个数、数据类型、顺序等）

⚠️注意：函数返回值不同不能作为函数重载的条件

## 函数重载注意事项